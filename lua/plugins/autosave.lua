return {
  "brianhuster/autosave.nvim",
  -- Optional: Enable autosave by default when the plugin loads
  -- config = function()
  --   require("autosave").setup({
  --     -- Your custom options here
  --   })
  -- end,
}
